<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net472</TargetFramework>
    <OutputType>Library</OutputType>
    <AppDesignerFolder>Properties</AppDesignerFolder>
    <RootNamespace>HighQualityTextures</RootNamespace>
    <AssemblyName>HighQualityTextures</AssemblyName>
    <Nullable>disable</Nullable>
    <!-- OutputPaths für alle Versionen zentralisiert -->
    <OutputPathBase>..\$(Version)\Assemblies</OutputPathBase>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
  </PropertyGroup>

  <!-- Konfigurationen für Versionen (1.0–1.3) -->
  <PropertyGroup Condition="'$(Configuration)' == '1.0'">
    <OutputPath>$(OutputPathBase)\</OutputPath>
    <Version>1.0</Version>
  </PropertyGroup>
  <PropertyGroup Condition="'$(Configuration)' == '1.3'">
    <OutputPath>$(OutputPathBase)\net472\</OutputPath> <!-- Für 1.3-spezifische Struktur -->
    <Version>1.3</Version>
  </PropertyGroup>

  <!-- Unity-Referenzen (Pfade anpassen, da Projekt im "Source"-Ordner liegt) -->
  <ItemGroup>
    <Reference Include="Assembly-CSharp">
      <HintPath>..\..\..\RimWorldWin64_Data\Managed\Assembly-CSharp.dll</HintPath>
      <Private>False</Private>
    </Reference>
    <Reference Include="UnityEngine.CoreModule">
      <HintPath>..\..\..\RimWorldWin64_Data\Managed\UnityEngine.CoreModule.dll</HintPath>
      <Private>False</Private>
    </Reference>
    <!-- Füge alle anderen Unity-Module analog hinzu -->
  </ItemGroup>

  <!-- Quelldateien -->
  <ItemGroup>
    <Compile Include="**/*.cs" Exclude="obj/**" /> <!-- Automatische Erkennung -->
  </ItemGroup>

  <!-- NuGet-Pakete -->
  <ItemGroup>
    <PackageReference Include="Lib.Harmony" Version="2.3.5" />
  </ItemGroup>
</Project>